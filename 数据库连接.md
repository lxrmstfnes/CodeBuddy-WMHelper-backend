# 数据库连接（本地开发环境）

> 业务库：OceanBase CE（MySQL 租户模式），跑在 Docker 容器 `wealth-ob` 中。
> 本文档记录**查看/操作数据库数据**的两种方式：网页版（推荐）与命令行。
> 配套文档：`DOCKER_SETUP.md`（镜像与端口规划）、`sql/01-schema.sql`（表结构）。

---

## 方式一：Adminer 网页版（推荐，浏览/改数据最方便）

### 1. 打开页面

浏览器访问：**http://localhost:8081**

### 2. 登录（参数照抄）

| 字段 | 填写值 | 备注 |
| --- | --- | --- |
| 系统 | `MySQL` | 默认即是 |
| 服务器 | `wealth-ob:2881` | ⚠️ **不是 localhost**：Adminer 容器通过 Docker 内网按容器名连 OB |
| 用户名 | `root@test` | 格式：用户名@租户名 |
| 密码 | `root123` | 本地开发密码（原为设空，因 Adminer 拦截空密码登录而设置） |
| 数据库 | `wealth_copilot` | 业务库 |

### 3. 查看数据

登录成功后：

1. 左侧栏列出 9 张表：`product`（理财产品）、`company`（理财子公司）、`client_persona`（客群画像）、`market_report` / `market_report_card`（快报）、`role_play_script` / `script_round`（对练剧本与轮次）、`forbidden_word`（违禁词）、`mot_event`（商机事件）
2. 点表名 → 点「**选择数据**」（select data）即可浏览全部行
3. 常用操作：左侧「SQL 命令」可手写查询；每行可点「编辑/删除」；「插入」可加测试数据

> 想刷新 MoT「今日」时间线数据：种子数据 `event_date` 是初始化当天的日期，
> 改数据时把 `mot_event.event_date` 改成当天即可在前端看到。

---

## 方式二：命令行 obclient

```bash
# 交互式进入（在 OB 容器内执行，免装客户端）
docker exec -it wealth-ob obclient -h127.0.0.1 -P2881 -uroot@test -proot123 \
  --default-character-set=utf8mb4 wealth_copilot

# 单条查询示例
docker exec wealth-ob obclient -h127.0.0.1 -P2881 -uroot@test -proot123 \
  --default-character-set=utf8mb4 -e "SELECT id, company, name FROM wealth_copilot.product;"
```

> ⚠️ `--default-character-set=utf8mb4` **必须带**，否则中文显示为 `???`（只是显示问题，数据本身没坏）。

---

## 后端服务连接配置（备查）

后端 `backend/.env`（已被 .gitignore 忽略，不进 git）：

```env
DB_HOST=localhost
DB_PORT=2881
DB_USER=root@test
DB_PASSWORD=root123
DB_NAME=wealth_copilot
```

JDBC 写法（未来 SOFABoot 化时用）：

```
url: jdbc:mysql://localhost:2881/wealth_copilot?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=UTF-8
username: root@test
password: root123
```

---

## 日常运维速查

```bash
cd CodeBuddy-WMHelper-backend/

docker compose up -d        # 启动全部（OB / Redis / Adminer）
docker compose down         # 停止（数据保留在 volume 中）
docker compose ps           # 查看容器状态
docker logs -f wealth-ob    # 看 OB 日志（出现 boot success 才算就绪）

./init-db.sh                # 重置数据库：删表重建 + 重灌种子数据（仅限开发环境！）
```

---

## 常见问题

| 现象 | 原因与处理 |
| --- | --- |
| 8081 打不开 | Adminer 容器没起：`docker compose up -d adminer` |
| 登录后中文是 `???` | 仅命令行问题，加 `--default-character-set=utf8mb4`；Adminer 无此问题 |
| 后端连不上库 | 检查 `.env` 的 `DB_PASSWORD` 是否为 `root123`；`docker compose ps` 确认 `wealth-ob` 在跑 |
| 连接报密码错误 | 若执行过 `docker compose down -v`（清卷），密码会被重置为空，需重新执行：`docker exec wealth-ob obclient -h127.0.0.1 -P2881 -uroot@test -e "ALTER USER root IDENTIFIED BY 'root123';"` |

---

*整理时间：2026-09-14 · 密码与端口如有调整请同步更新本文档*
